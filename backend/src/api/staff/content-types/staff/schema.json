{
  "kind": "collectionType",
  "collectionName": "staff_old",
  "info": {
    "singularName": "staff-old",
    "pluralName": "staff-old",
    "displayName": "Staff (old)"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "first_name": {
      "type": "string",
      "required": true
    },
    "last_name": {
      "type": "string",
      "required": true
    },
    "role": {
      "type": "enumeration",
      "enum": ["Assistant coach", "Fitness coach", "Doctor", "Physio"]
    }
  }
}
